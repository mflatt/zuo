#lang zuo/private/defmacromod

(require "defmacromod/and-or.zuo")

(provide any?
         void?
         boolean?)

;; mainly for contracts in the docs:
(define any? (lambda (v) #t))
(define void? (lambda (v) (eq? v (void))))

(define boolean?
  (lambda (v)
    (or (eq? v #t) (eq? v #f))))
